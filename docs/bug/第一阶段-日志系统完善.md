- 经过对文档和源码的逐项核对，**第一阶段（🔴 高优先级）的所有三个步骤的日志改造已经全部完成**，无需再进行任何修改。以下是详细的核对结果：

  ---

  ## 第一阶段完成状态核对

  ### Step 1: `infrastructure/FakePLC.h` — 所有 `processCommand` 重载

  | 命令                       | 入口 DEBUG                                 | WARN（拒绝）                          | 执行成功 DEBUG       | 状态 |
  | -------------------------- | ------------------------------------------ | ------------------------------------- | -------------------- | ---- |
  | `StopCommand`              | ✅ `"process: axis=... cmd=Stop state=..."` | ✅ `"Stop ignored: ... not in motion"` | —                    | ✅    |
  | `EnableCommand`            | ✅ 含 `active=true/false`                   | ✅ `"EMERGENCY STOP ACTIVE"`           | —                    | ✅    |
  | `JogCommand`               | ✅ 含 `dir/active`                          | ✅ 急停 / 龙门 / 状态三种              | —                    | ✅    |
  | `MoveCommand`              | ✅ 含 `type/target`                         | ✅ 急停 / 龙门 / 状态三种              | —                    | ✅    |
  | `ZeroAbsoluteCommand`      | ✅ 含 `absPos`                              | ✅ 状态拒绝                            | ✅ `"abs ... → 0.0"`  | ✅    |
  | `SetRelativeZeroCommand`   | ✅ 含 `currentBase`                         | ✅ 状态拒绝                            | ✅ `"base ... → ..."` | ✅    |
  | `ClearRelativeZeroCommand` | ✅ 含 `currentBase`                         | ✅ 状态拒绝                            | ✅ `"base ... → 0.0"` | ✅    |
  | `SetJogVelocityCommand`    | ✅ 含 `velocity`                            | —                                     | —                    | ✅    |
  | `SetMoveVelocityCommand`   | ✅ 含 `velocity`                            | —                                     | —                    | ✅    |
  | `GantryCouplingCommand`    | ✅                                          | —                                     | —                    | ✅    |
  | `GantryPowerCommand`       | ✅                                          | —                                     | —                    | ✅    |

  ### Step 2: `domain/entity/Axis.cpp` — `applyFeedback()` 全部 9 个闭环分支

  | 分支                 | 消费 DEBUG                                               | 诊断                                   | 状态 |
  | -------------------- | -------------------------------------------------------- | -------------------------------------- | ---- |
  | 1. 限位熔断          | ✅ `"LIMIT FUSE — clearing motion intent"`                | —                                      | ✅    |
  | 2. 运动→清理 Jog     | ✅ `"clearing Jog intent"`                                | —                                      | ✅    |
  | 3. 静止→清理 Stop    | ✅ `"clearing Stop intent"`                               | —                                      | ✅    |
  | 4. ZeroAbsolute      | ✅ `"ZeroAbsolute CLOSED"`                                | —                                      | ✅    |
  | 5. SetRelativeZero   | ✅ `"SetRelativeZero CLOSED"`                             | ✅ 节流诊断 `"SetRelativeZero waiting"` | ✅    |
  | 6. ClearRelativeZero | ✅ `"ClearRelativeZero CLOSED"`                           | —                                      | ✅    |
  | 7. Move 收敛         | ✅ `"Move CLOSED"`                                        | —                                      | ✅    |
  | 8. Enable/Disable    | ✅ `"Enable CLOSED"` / `"Disable CLOSED"`                 | —                                      | ✅    |
  | 9. Velocity          | ✅ `"SetJogVelocity CLOSED"` / `"SetMoveVelocity CLOSED"` | —                                      | ✅    |

  ### Step 3: `domain/entity/Axis.cpp` — 全部 11 个准入方法

  | 方法                   | 入口 DEBUG | 拒绝 DEBUG                                        | 通过 DEBUG | 幂等 DEBUG | 状态 |
  | ---------------------- | ---------- | ------------------------------------------------- | ---------- | ---------- | ---- |
  | `enable`               | ✅          | ✅ InvalidState / AlreadyMoving                    | ✅          | ✅          | ✅    |
  | `jog`                  | ✅          | ✅ AlreadyMoving / InvalidState / 限位(bit+soft)   | ✅          | —          | ✅    |
  | `stopJog`              | ✅          | —                                                 | ✅          | —          | ✅    |
  | `moveAbsolute`         | ✅          | ✅ AlreadyMoving / InvalidState / 限位(bit+target) | ✅          | —          | ✅    |
  | `moveRelative`         | ✅          | ✅ AlreadyMoving / InvalidState / 限位(bit+target) | ✅          | —          | ✅    |
  | `stop`                 | ✅          | —                                                 | ✅          | —          | ✅    |
  | `zeroAbsolutePosition` | ✅          | ✅ InvalidState                                    | ✅          | —          | ✅    |
  | `setRelativeZero`      | ✅          | ✅ InvalidState                                    | ✅          | —          | ✅    |
  | `clearRelativeZero`    | ✅          | ✅ InvalidState                                    | ✅          | —          | ✅    |
  | `setJogVelocity`       | ✅          | ✅ InvalidArgument / InvalidState                  | ✅          | —          | ✅    |
  | `setMoveVelocity`      | ✅          | ✅ InvalidArgument / InvalidState                  | ✅          | —          | ✅    |

  ---

  **结论：第一阶段所有修改已就位，没有遗漏。** 两条核心文件 (`FakePLC.h` + `Axis.cpp`) 中的日志覆盖已达到文档要求，无需进一步修改。文档中提到的第二阶段的若干项（如 `FakeAxisDriver.h` 的 `pollFeedback` 节流日志、`AxisViewModelCore.cpp` 的 `consumePendingCommands` TraceScope 贯穿等）也已部分或全部实现。
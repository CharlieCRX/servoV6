// ============================================================================
// DomainVnextAnchor.cpp —— 空锚点
// ============================================================================
// 保证 domain_vnext 库类型从第一天起固定为 STATIC，后续新增 .cpp 编译单元时
// 无需修改 add_library(STATIC) 定义。P1 model 全部为纯头文件值对象，无编译单元。
// ============================================================================
namespace domain_vnext {
// 锚点：显式引用以避免空编译单元被优化掉（无 I/O、无业务）。
inline int domain_vnext_anchor_v1() noexcept { return 0; }
}  // namespace domain_vnext
